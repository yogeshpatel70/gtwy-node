// Leaf module — no imports. Safe from circular-dependency races.
// Keep in sync with Python's `tag_keys`.
export const tag_keys = {
  tool: "tool",
  apikey: "apikey",
  folder: "folder",
  connected_agent: "connected_agent",
  wrapper: "wrapper",
  agent: "agent",
  version: "version",
  rag: "rag"
};
